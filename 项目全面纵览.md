# 落球法液体黏度自动测量系统 — 项目全面纵览

> 生成日期：2026/05/17  
> 项目根目录：`D:\pycharm\project\wulijingsai`

---

## 一、项目定位

基于**机器视觉自动追踪与误差修正**的落球法液体黏度测量实验改进。

核心目标：将传统落球法中最主观、最容易出错的**终端速度判断与计时**过程，改造为**自动追踪、自动判稳、自动修正**的测量流程。

---

## 二、当前进度总览

| 阶段 | 内容 | 进度 |
|------|------|------|
| 第1阶段 | 环境搭建 + 项目框架 | **100%** ✅ |
| 第2阶段 | OpenCV 球心识别 | **100%** ✅（远超原计划，已实现多方法融合评分） |
| 第3阶段 | 自动判定终端速度 | **100%** ✅（滑动窗口 + 兜底双算法） |
| 第4阶段 | 三方法对比实验 | **0%** ⏳（待 A 同学提供真实实验数据） |
| 第5阶段 | 黏度计算 + 修正模型 | **100%** ✅（理想 Stokes + 壁面修正 + 雷诺数验证） |
| 第6-7阶段 | 答辩准备 | **0%** ⏳ |

**当前瓶颈**：仅有 AI 生成的测试视频（非真实物理实验），终端速度区判定在 AI 视频上失败（因 y 位移仅 0.0026m，排除起止段后无有效数据）。**等待 A 同学拍摄真实落球实验视频**。

---

## 三、代码结构

```
D:\pycharm\project\wulijingsai\
│
├── B\                              # 主项目（核心开发目录）
│   ├── main.py                     # 命令行入口（完整分析管线）
│   ├── gui_app.py                  # GUI 入口（PySide6）
│   ├── config.py                   # 旧版硬编码配置（已被 config.yaml 替代）
│   ├── config.yaml                 # YAML 配置文件
│   ├── ball_tracker.py             # 旧版追踪器（早期原型，已被 src/ 模块替代）
│   ├── plotter.py                  # 旧版绘图模块（早期原型）
│   ├── requirements.txt            # Python 依赖
│   ├── README.md                   # 项目文档
│   │
│   ├── src/                        # 核心源码（模块化重构版）
│   │   ├── ball_detector.py        # ★ 小球检测（多方法融合+综合评分）
│   │   ├── tracking.py             # ★ 逐帧追踪（预测容错+后处理）
│   │   ├── velocity.py             # 速度计算（滑动窗口 y-t 拟合）
│   │   ├── terminal_region.py      # ★ 终端速度区自动判定
│   │   ├── viscosity.py            # 黏度计算（Stokes + 壁面修正）
│   │   ├── plotting.py             # 图表输出（y-t/v-t/拟合图）
│   │   ├── report.py               # 结果摘要报告
│   │   ├── video_io.py             # 视频读写封装
│   │   ├── utils.py                # 工具函数
│   │   └── gui/                    # GUI 界面模块
│   │       ├── main_window.py      # 主窗口（三段式布局）
│   │       ├── parameter_panel.py  # 右侧参数面板
│   │       ├── video_widget.py     # 视频预览组件
│   │       ├── result_tabs.py      # 结果展示 Tab
│   │       ├── worker.py           # 后台分析线程
│   │       └── dialogs.py          # 对话框
│   │
│   ├── videos/                     # 视频输入
│   │   └── jimeng-*.mp4            # AI 生成测试视频
│   │
│   ├── data/results/               # 分析输出
│   │   ├── trajectory.csv          # 轨迹数据
│   │   ├── velocity.csv            # 速度数据
│   │   ├── candidate_debug.csv     # 逐帧候选点评分明细
│   │   ├── frame_debug.csv         # 逐帧检测日志
│   │   ├── marked_video.mp4        # 标注视频
│   │   ├── trajectory_plot.png     # y-t 轨迹图
│   │   ├── velocity_plot.png       # v-t 速度图
│   │   ├── fit_plot.png            # 终端区拟合图
│   │   └── result_summary.txt      # 结果摘要
│   │
│   ├── config/settings.json        # GUI 持久化参数
│   └── output/                     # 旧版输出目录
│
├── B1\                             # 早期单张截图原型（ball_detector 前身）
│   └── detect_ball.py              # 单帧检测：Blob + 轮廓 + 霍夫圆
│
└── data/results/                   # 共享输出目录（符号链接/备份）
```

---

## 四、核心模块实现详解

### 4.1 小球检测模块 [ball_detector.py]

最复杂、迭代最多的模块（~1550 行），采用**多方法融合 + 综合评分**策略。

**检测方法（按优先级）**：
1. **背景差分法** (bg_sub) — 视频模式首选，运动目标检测，有运动奖励分
2. **SimpleBlobDetector** — 通用 blob 检测，对比度验证
3. **Otsu 阈值 + 轮廓检测** — 后备方案
4. **自适应阈值** — 大球模式启用
5. **HoughCircles** — 大球模式兜底

**综合评分系统（v5 运动优先版）**：

| 维度 | 满分 | 说明 |
|------|------|------|
| diff_score | 25 | 背景差分值，运动的候选高分，静止(刻度线/管壁)低分 |
| size_score | 25 | 尺寸匹配度（高斯型衰减） |
| pred_score | 20 | 预测位置接近度 |
| continuity | 15 | 运动连续性（方向/速度） |
| iso_score | 15 | 孤立小黑点评分 |
| axis_score | 10 | 下落中心线接近度 |
| cb_score | 10 | 中心运动带 |
| motion_bonus | 5 | bg_sub 来源奖励 |
| contrast_score | 5 | 对比度 |
| ll_penalty | -20~0 | 长线结构扣分 |

**追踪机制**：
- 非对称搜索窗口（x ± 60px, y: -20 ~ +120px）
- 速度预测下一帧位置（最多连续预测 10 帧）
- 动态扩大搜索窗口（丢失 3/8/20 帧逐步扩大）
- 长线结构排除（刻度线/弧线候选扣分）

### 4.2 视频追踪模块 [tracking.py]

**追踪管线**：
1. 构建背景模型（前 50 帧中值合成）
2. 动态计算检测参数（基于球径/比例尺）
3. 自动估计下落中心线（前 15 个有效点 X 中位数）
4. 逐帧检测 → 异常过滤（跳变/x 通道/y 回跳）
5. 后处理：短间隙插值（≤3 帧）+ 离群点剔除
6. 统计：有效识别率、最长连续段、段质量评估

**追踪状态机**：
- `tracking` → `lost` → `reacquire` → `stopped`

### 4.3 终端速度判定 [terminal_region.py]

**v2 算法（主流程）**：
1. 滑动窗口（默认 0.8s）在有效轨迹上做 y-t 线性拟合 → 得 v_t 和 R²
2. 连续 R² 合格窗口组成候选群组
3. 对每个群组计算 Cv = σ(v_t) / mean(v_t)
4. 选 Cv < 阈值 + R² > 阈值 + 持续时间最长的群组

**兜底算法**：
- 当主流程未找到时，用更宽松判据（仅 R² + x 稳定性 + y 单调性）
- 不依赖 Cv，适用于数据波动但整体线性的场景

### 4.4 黏度计算 [viscosity.py]

**公式**：
```
η_basic = 2 * r² * g * (ρ_s - ρ_l) / (9 * v_t)

修正因子 correction = (1 + 2.4 * r/R) * (1 + 3.3 * r/h)
η_wall = η_basic / correction

雷诺数 Re = 2 * r * v_t * ρ_l / η_wall
```

**符号约定**：公式中的 r, R, h 均为**半径/高度值**（非直径）。

### 4.5 GUI 应用 [src/gui/]

基于 PySide6 的三段式桌面应用：
- **左侧**：视频预览（QPainter 绘制，支持 ROI 框选、比例尺标定、播放/逐帧）
- **右侧**：参数面板（分组折叠，禁止滚轮误触）
- **下方**：结果展示（y-t/v-t/拟合图 Tab + 黏度结果表 + 日志）

工作线程 AnalysisWorker 避免 GUI 卡死。

---

## 五、技术栈

| 技术 | 用途 |
|------|------|
| Python 3.10.11 | 语言 |
| OpenCV 4.13.0 | 图像处理（核心） |
| NumPy 2.2.6 | 数值计算 |
| Pandas 2.3.3 | 数据处理 |
| Matplotlib 3.10.9 | 可视化 |
| SciPy 1.14.1 | 科学计算 |
| PySide6 6.11.1 | GUI 框架 |
| PyYAML | 配置文件 |

---

## 六、运行结果

已对 AI 生成的测试视频（240帧，240fps）完成全流程测试：

| 指标 | 值 |
|------|-----|
| 总帧数 | 241 |
| 有效识别帧数 | 240 |
| 有效识别率 | 99.6% |
| 点类型(raw/predicted/interpolated) | raw 为主，少量 predicted/interpolated 填补 |
| 追踪状态 | tracking 为主，偶有 lost 后快速恢复 |
| 终端速度判定 | ❌ 失败（AI 视频 y 位移仅 0.0026m，排除首尾后无数据） |

**结论**：程序管线已完全打通，但需要**真实物理实验视频**进行验证。

---

## 七、文件清单（核心源文件，不含第三方库）

| 文件 | 行数 | 功能 |
|------|------|------|
| B/src/ball_detector.py | ~1550 | 小球检测核心 |
| B/src/tracking.py | ~1544 | 视频追踪管线 |
| B/src/terminal_region.py | ~546 | 终端速度判定 |
| B/src/viscosity.py | ~189 | 黏度计算 |
| B/src/velocity.py | ~189 | 速度计算 |
| B/src/plotting.py | ~297 | 图表输出 |
| B/src/report.py | ~161 | 结果报告 |
| B/src/utils.py | ~172 | 工具函数 |
| B/src/video_io.py | ~95 | 视频 I/O |
| B/src/gui/main_window.py | ~786 | 主窗口 |
| B/src/gui/parameter_panel.py | ~878 | 参数面板 |
| B/src/gui/result_tabs.py | ~386 | 结果 Tab |
| B/src/gui/video_widget.py | ~600+ | 视频组件 |
| B/src/gui/worker.py | ~307 | 工作线程 |
| B/main.py | ~268 | CLI 入口 |
| B/gui_app.py | ~45 | GUI 入口 |
| B1/detect_ball.py | ~288 | 单帧检测原型 |
| **总计** | **~7300+** | |
