# B 同学 — 程序与数据负责人 任务计划与实现记录

> 创建日期：2026/05/12
> 项目：落球法液体黏度自动测量系统

---

## 一、项目总定位

基于机器视觉自动追踪与误差修正的落球法液体黏度测量实验改进。

核心目标：将传统落球法中最主观、最容易出错的终端速度判断与计时过程，改造为 **自动追踪、自动判稳、自动修正和可输出不确定度** 的测量流程。

---

## 二、B 同学分工

| 角色 | 核心任务 | 最终交付 |
|------|----------|----------|
| **程序与数据负责人** | OpenCV 识别球心、自动判定终端速度、数据处理、画图 | Python 程序、球心轨迹图、速度曲线、黏度结果表 |

### B 的答辩重点
程序如何从视频得到球心坐标，如何自动判断终端速度区，如何保证不是人为挑选数据。

---

## 三、技术选型

| 项 | 选择 | 原因 |
|---|---|---|
| **语言** | Python 3.10.11 | 团队已有 Python 基础，生态成熟 |
| **图像处理** | OpenCV (`cv2`) | 核心依赖，轮廓检测/霍夫圆/背景差分都内置 |
| **数据分析** | NumPy + SciPy | 数值计算、平滑滤波、线性拟合 |
| **可视化** | Matplotlib | 轨迹图、速度图、对比柱状图 |
| **输出格式** | CSV | 通用数据交换，Excel/Origin 可直接打开 |

**不采用**深度学习框架（如 YOLO）——传统 CV 方法在固定背景下更稳定、可解释、无 GPU 依赖。

---

## 四、软件架构

处理管线（逐模块递进）：

```
视频输入 → 标定(mm/pixel) → 预处理(灰度/背景扣除/二值化)
  → 球心检测(轮廓/霍夫圆) → 输出坐标 CSV
  → y-t 轨迹 → v-t 速度 → 自动判稳(双判据)
  → 线性拟合 → 终端速度 v_t → 黏度计算(理想+修正)
  → 结果图表 + 对比柱状图
```

### 文件结构

```
D:\pycharm\project\wulijingsai\B\          # 开发目录（PyCharm）
├── config.py              # 全局参数（阈值、判据、物理参数）
├── ball_tracker.py        # 核心：视频读取 → 球心检测 → CSV
├── plotter.py             # 可视化：y-t/v-t/对比柱状图
├── main.py                # 命令行入口
├── requirements.txt       # 依赖清单
├── .gitignore
├── videos/                # 视频输入
├── data/                  # CSV 输出
└── output/                # 图表输出
```

---

## 五、阶段计划与进度

### 第 1 阶段（5/10—5/14）装置搭建与试拍
- ✅ 环境搭建完成：Python 3.10.11 + OpenCV + NumPy + Matplotlib + SciPy + Pandas
- ✅ 项目框架代码完成：ball_tracker.py / config.py / main.py / plotter.py
- ⏳ 等待 A 同学拍出测试视频

### 第 2 阶段（5/15—5/19）OpenCV 球心识别 ← **当前阶段**
- [ ] 读取视频，获取帧率/总帧数/分辨率
- [ ] 图像预处理（灰度化、阈值分割、背景扣除）
- [ ] 识别小球轮廓并提取球心
- [ ] 保存每帧 (x, y, t) 数据到 CSV
- [ ] 绘制 y-t 轨迹图
- **验收标准**：程序能自动识别大部分帧中的小球；输出球心坐标表；y-t 曲线整体连续。

### 第 3 阶段（5/20—5/24）自动判定终端速度
- [ ] 由 y(t) 计算 v(t)，平滑去噪
- [ ] 建立判稳规则（Cv + R² 双判据）
- [ ] 自动寻找匀速区
- [ ] 线性拟合得到终端速度 v_t
- [ ] 输出 v_t、R²、区间、基础黏度

### 第 4 阶段（5/25—5/29）三方法对比实验
- [ ] 批量处理视频数据
- [ ] 汇总秒表法 / Tracker 法 / OpenCV 法数据
- [ ] 绘制三方法对比柱状图

### 第 5 阶段（5/30—6/2）修正模型与图表
- [ ] 理想黏度计算
- [ ] 壁面修正 / 雷诺数修正
- [ ] 修正前后对比图
- [ ] 原始数据包整理

### 第 6—7 阶段（6/3—6/7）答辩准备
- [ ] 程序截图与演示视频
- [ ] 现场演示准备

---

## 六、代码模块说明

### config.py
集中管理所有可调参数：
- **视频参数**：路径、跳帧、最大帧数
- **预处理参数**：高斯模糊核、阈值方法/阈值、背景扣除开关
- **检测参数**：检测方法（contour/hough）、半径范围、圆度阈值
- **标定参数**：标尺实际长度
- **判稳参数**（Phase 3）：滑动窗口、Cv 阈值、R² 阈值
- **物理参数**（Phase 5）：球径、密度、重力、容器尺寸

### ball_tracker.py
`BallTracker` 类 — 核心追踪管线：
- `open_video()` — 打开视频，读取属性
- `calibrate()` — 像素-实际长度标定
- `build_background()` — 前 N 帧中值背景模型
- `preprocess()` — 灰度 → 高斯模糊 → 背景扣除 → 二值化
- `detect_ball()` — 轮廓检测 → 面积/圆度筛选 → 球心输出
- `run()` — 完整管线：读取每一帧 → 处理 → 保存 CSV

### main.py
命令行入口，支持子命令：
- `python main.py track --video xxx.mp4` — 完整追踪
- `python main.py demo --video xxx.mp4` — 单帧测试

### plotter.py
独立画图函数，每图一个函数：
- `plot_yt()` — y-t 位移曲线
- `plot_vt()` — v-t 速度曲线
- `plot_terminal_zone()` — 终端速度区标注
- `plot_comparison()` — 三方法对比柱状图
- `plot_correction()` — 修正前后对比

---

## 七、判稳双判据（核心算法）

```
判据 1: Cv = σ_v / mean(v) < 5%    （速度变异系数）
判据 2: y-t 线性拟合 R² > 0.995    （拟合优度）

方法: 滑动窗口遍历，取满足双判据的最长连续区间。
```

---

## 八、关键风险与应对

| 风险 | 表现 | 应对 |
|------|------|------|
| 小球背景对比度低 | 轮廓检测不到 | 换背光板 / 调阈值 / 改用霍夫圆 |
| 反光/气泡干扰 | 误识别 | 面积筛选 + 圆度筛选 + 位置连续性过滤 |
| 小球碰壁 | y-t 异常 | Cv 判据自然筛掉异常段 |
| 帧率不足 | 匀速区数据点少 | 要求 120fps 以上慢动作模式 |

**兜底方案**：若自动追踪不稳定，先退回手动选帧标定 + 线性插值，保证进度不被卡。

---

## 九、环境配置

```
Python:     D:\python3.10.11\python.exe
项目目录:   D:\pycharm\project\wulijingsai\B\
依赖:
  opencv-python==4.13.0.92
  numpy==2.2.6
  matplotlib==3.10.9
  scipy==1.15.3
  pandas==2.3.3
```

---

## 十、立即开始（TODO）

1. 用 PyCharm 打开 `D:\pycharm\project\wulijingsai`，设 Python 解释器为 `D:\python3.10.11\python.exe`
2. A 同学拍完视频后，放入 `B/videos/`
3. 运行测试：`python B/main.py demo --video B/videos/test_01.mp4`
4. 根据检测效果调整 `config.py` 中的阈值参数
5. 调通后执行完整追踪：`python B/main.py track --video B/videos/test_01.mp4`
